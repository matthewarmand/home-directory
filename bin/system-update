#!/bin/bash

require_dependency() {
  if ! command -v "$1" > /dev/null; then
    echo "$1 is required"
    exit 1
  fi
}

bold_message() {
  echo -e "\033[1m$1\033[0m"
}

# check for dependencies
require_dependency arch-audit
require_dependency jq
require_dependency paccache
require_dependency pacdiff
require_dependency yay

# Check news. I'd love to smart-parse this... we'll see how feasible that is another day
# This would scan for "manual intervention" blocks and ignore them if not installed. Prints all else
if news="$(script -q /dev/null -c 'yay -Pw')" && [ -n "$news" ]; then
  echo "$news"
  echo
  bold_message "Check above news for relevant manual interventions. Ready to continue? [y] "
  while true; do
    read -r yn
    case $yn in
      [Yy]* ) break;;
      * ) echo "Enter y when ready to continue.";;
    esac
  done
fi

# Check security audit for fixed packages; this may make the upgrade more urgent/desirable
arch-audit -uc

# Full system update
yay -Syu

# Remove any orphaned packages
(yay -Qtdq && yay -Rns "$(yay -Qtdq)") || true

# Interactively resolve configuration files
sudo pacdiff

# Clear pacman and yay caches, keeping last 3
paccache_cmd="paccache -r -k 3 -c $(grep CacheDir /etc/pacman.conf | sed 's/.*= //')"
for d in "$(yay -Pg | jq -r .buildDir)"/*/; do
  paccache_cmd="$paccache_cmd -c $d"
done
bold_message "Cleaning package caches..."
$paccache_cmd

# Check for kernel upgrade; request restart
if [ "$(uname -r)" != "$(yay -Q linux | awk '{print $2}' | sed 's/\.arch/-arch/')" ]; then
  echo
  bold_message "Kernel has been upgraded; reboot now? [y/n] "
  while true; do
    read -r yn
    case $yn in
      [Yy]* ) reboot;;
      [Nn]* ) break;;
      * ) echo "Please answer y or n.";;
    esac
  done
fi
